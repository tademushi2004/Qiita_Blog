# SKILLBLOG.md v6.0：技術ブログ執筆の設計思想と厳格ルール（マスター仕様書）

## 0. 爆速執筆パイプライン（SDD: 仕様駆動執筆）
ブログ執筆において、手戻りを最小化し、少ない壁打ち回数で完成させるためのパイプライン。

1. **【ソース検証】 NotebookLM**:
    * 提供されたソース（資料）を読み込み、これから書くブログのテーマに対して「客観的・科学的な一次ソースが十分か」を自己評価させる。
    * 不足している場合、勝手に想像で補わず、**必ずDeep Research（検索代行）用のプロンプトを提案させる**こと。
2. **【設計図の完全合意】 NotebookLM**:
    * ソースが十分になった段階で、以下の5点を網羅した「設計図」を出力させ、人間が確定させる。
        1. **タイトル案（切り口を変えて10案出力）**
        2. **目次構成案**（各章で読者に要求する「前提知識の依存関係ツリー」を併記）
        3. **冒頭の引用ブロック（50文字紹介文）**
        4. **（連載の場合のみ）各回の末尾に配置する「まとめ」の構成**
        5. **記事末尾の免責事項**
3. **【初稿生成と部分修正】 NotebookLM**:
    * 確定した設計図に従い本文を生成する。この際、「初心者の理解しやすさ」「論理の繋がり」で自己採点（100点満点）を行わせ、95点未満なら自己修正させる（※無限ループ防止のため最大3回まで）。
    * **【重要】人間から修正依頼があった場合、指示された箇所以外の文章や構成は「絶対に」変更してはならない。**
4. **【フォーマットLinterとメタデータ】 Gemini（辛口Linter専用Gem）**:
    * 完成した原稿をGeminiに渡し、**「文体や内容のトーン（親切で温かみのある文章）には一切手を加えず」**、以下のチェックのみを実行させる。
        * マークダウンの構文エラー（太字のスペース、コードブロックの崩れ等）の修正。
        * プレースホルダー（画像挿入位置）の意図しない削除の復元。
        * **孫引きの検知**（書籍からの引用がある場合、元の論文を探して引用するよう警告を出す）。
        * 適切なQiitaタグ（5つ）とテーマの選定（本文とは隔離したコードブロックに出力）。
5. **【画像生成パイプライン】 Gemini & 筆者**:
    * 原稿が完全にFIXした後、人間が画像を挿入したい箇所（見出し2や3の下など柔軟に）にプレースホルダーを配置する。
    * 人間からの要求があった場合のみ、Geminiに画像生成プロンプトを作成させる。
    * **画風の絶対指定**: `Minimalist modern flat vector illustration, 16:9 aspect ratio. No text, no words, no alphabet letters, no numbers.`
    * **ファイル管理**: 生成したPNG画像はローカルのPythonスクリプトで一括で `avif` に変換し、指定のフォルダに格納して公開する。

## 1. 記事の構造と連載ルール（Macro Structure）
記事の形式（単発 / 連載）に応じて、以下の構造要件を厳格に切り替えること。

* **【全記事共通】冒頭のショートイントロダクション**
    * 記事の冒頭（目次と本文の間）には、ステップ2で合意した50文字程度の紹介文を引用ブロック（`>`）で必ず配置する。
* **【連載記事の特別処理】ナビゲーションと結合テスト**
    * **目次とリンク**: 冒頭に「全連載の目次一覧（現在地に『▶ 現在地：』を付与）」を設置。前後のブログ記事へのリンクは、必ず標準リンク記法 **`[記事の名前](URL)`** を使用する。
    * **各回のまとめ**: 連載の最終回「以外」の記事では、必ず記事の末尾（次回予告の直前）に、その回の要点を振り返る「まとめ」の章を配置する。
    * **結合テスト**: 前回の「次回予告」と今回の「本編の入り」が論理的に完全一致していること。

## 2. 論理の設計と構造（Logical Architecture）
* **時系列と依存関係の順守（前提の先出し）**
    * 読者が実際に手を動かす時系列に沿って章を配置する。ステップ2で作成した「依存関係ツリー」を逸脱する唐突な論理展開を禁ずる。

## 3. コア理念と文章術（Core Principles & Writing）
* **記号接地の最重視**: 無味乾燥な辞書的説明を避け、日常的なメタファーを用いて読者の身体感覚に接地させる。
* **親切で温かみのあるポッドキャスト風モノローグ**: 画面の向こうの「あなた」に直接語りかける文体を採用する。AIの過度なリライトによる「味気ない・あっさりしすぎた文章」への劣化を許容しない。
* **結論の先出し**: 一番おいしい部分（ゴール）を最初に見せる。

## 4. マークダウン・フォーマット厳格規約（Strict Formatting）
LLMが陥りやすいMarkdownの記述ミスを強制排除する。

* **出力の階層化**: 最終的なブログ本文は、必ず出力全体をバッククォート4つ（```` ```` ````）で囲むこと。
* **インラインコードとコードブロック**: ファイル名やコマンドはバッククォート1つ（`` `file.md` ``）、プログラムコード等はバッククォート3つ（```）と言語タグで囲む。
* **強調（太字）のスペース規則【超重要】**：
    * `**` の **内側には絶対にスペースを入れない**。
    * 日本語の文章中で太字にする場合、記号の **外側（前後）** に半角スペースを空ける。
* **図表の配置と画像パスの指定フォーマット（avif運用）**：
    * 画像は必要な箇所に柔軟に配置してよい（Linterが勝手に削除することを禁ずる）。
    * 連番は記事の先頭から「01, 02...」とゼロパディングで付与する。
    * PNGからAVIFへの変換を前提とし、画像リンクは以下の形式とする。
        * **【連載記事】** `![図解の説明テキスト](avif/記事名_記事番号-連番_image.avif)`
        * **【単発記事】** `![図解の説明テキスト](avif/記事名-連番_image.avif)`
* **情報量のコントロール**: 補足的な長文データは `<details><summary>概要テキスト</summary>詳細テキスト</details>` タグを用いて折りたたむ。

## 5. ファクトチェックと参考文献（Compliance & Citations）
* **孫引きの絶対禁止**: 「一次ソース（元の論文等）」を常に引用すること。
* **免責事項の絶対配置**: 記事末尾（参考文献の前）に必ず免責事項を引用ブロック（`>`）で明記する。内容は記事のスコープに合わせて適切に調整すること。
* **参考文献リストのフォーマットとソート**：
    * 参照元は脚注形式（`[^1]`）で本文中の該当箇所に付与し、末尾のリストは **「本文に出現した順番」で自動ソート** する。
    * **Web記事**: `[^1]: 著者名, "記事タイトル", メディア名. URL (参照 YYYY-MM-DD)`
    * **書籍**: `[^2]: 著者名 (発行年). 『書籍名』 出版社名.`
    * **論文**: `[^3]: 著者名 (発表年). "論文タイトル", 掲載誌名, 巻(号), ページ.`